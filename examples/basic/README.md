# Basic Examples

Simple workflow examples demonstrating core Workflow Engine concepts.

## Examples

### Simple Workflow

A basic workflow with a trigger and a single node.

See: [simple-workflow.ts](./simple-workflow.ts)

### Multi-Node Workflow

A workflow with multiple connected nodes.

See: [multi-node-workflow.ts](./multi-node-workflow.ts)

### Scheduled Workflow

A workflow triggered on a schedule.

See: [scheduled-workflow.ts](./scheduled-workflow.ts)

### Error Handling

A workflow demonstrating error handling.

See: [error-handling.ts](./error-handling.ts)

### Retry Mechanism

A workflow demonstrating retry mechanisms.

See: [retry-mechanism.ts](./retry-mechanism.ts)

## Running Examples

1. Navigate to the example directory
2. Install dependencies: `yarn install`
3. Build: `yarn build`
4. Run: `node dist/example.js`

## Prerequisites

- Node.js >= 18.0.0
- Yarn package manager
- Workflow Engine installed

