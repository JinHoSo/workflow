{
  "name": "workflow-engine",
  "version": "0.1.0",
  "private": true,
  "description": "KNIME/n8n-style workflow engine with TypeScript",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @workflow/interfaces build && yarn workspace @workflow/schemas build && yarn workspace @workflow/secrets build && yarn workspace @workflow/protocols build && yarn workspace @workflow/execution build && yarn workspace @workflow/plugins build && yarn workspace @workflow/core build && yarn workspace @workflow/nodes build && yarn workspace @workflow/cli build && yarn workspace @workflow/test-utils build",
    "build:watch": "echo 'Use individual package build:watch scripts'",
    "test": "echo 'Use individual package test scripts'",
    "test:watch": "echo 'Use individual package test:watch scripts'",
    "test:coverage": "echo 'Use individual package test:coverage scripts'",
    "lint": "echo 'Use individual package lint scripts'",
    "lint:fix": "echo 'Use individual package lint:fix scripts'",
    "typecheck": "echo 'Use individual package typecheck scripts'",
    "clean": "find packages -name dist -type d -exec rm -rf {} + && find packages -name tsconfig.tsbuildinfo -delete"
  },
  "keywords": [
    "workflow",
    "automation",
    "nodes",
    "pipeline"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "@types/sqlite3": "^5.1.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
